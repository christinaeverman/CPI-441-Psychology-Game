using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Disable_Room : MonoBehaviour
{
    public LoadRoom[] roomDoors;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (PlayerPrefs.GetInt("FoundAllLevel1", 0) == 1)
        {
            Debug.Log("Disable Room");
            roomDoors[1].roomDisabled = true;
        }
        if (PlayerPrefs.GetInt("FoundAllLevel2", 0) == 1)
        {
            Debug.Log("Disable Room");
            roomDoors[2].roomDisabled = true;
        }
        if (PlayerPrefs.GetInt("FoundAllLevel3", 0) == 1)
        {
            Debug.Log("Disable Room");
            roomDoors[3].roomDisabled = true;
        }
        if (PlayerPrefs.GetInt("FoundAllLevel4", 0) == 1)
        {
            Debug.Log("Disable Room");
            roomDoors[4].roomDisabled = true;
        }
        if (PlayerPrefs.GetInt("FoundAllLevel5", 0) == 1)
        {
            Debug.Log("Disable Room");
            roomDoors[5].roomDisabled = true;
        }
        */
    }
}
